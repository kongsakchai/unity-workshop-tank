using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Title : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    void Update()
    {
        
    }

    void SaveName()
    {
        // PlayerPrefs.SetInt(ชื่อที่ต้องการบันทึก,ค่าที่จะบันทึก)
    }
}
